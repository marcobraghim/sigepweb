name: sigepweb
description: Utilize serviços dos Correios no seu App como buscar CEP ou calcular preços e prazos de encomendas
version: 2.0.1-beta
repository: https://github.com/marcobraghim/sigepweb
issue_tracker: https://github.com/marcobraghim/sigepweb/issues

environment:
  sdk: ">=2.12.0 <3.10.5"

dependencies:
  xml2json: ^6.1.0
  dio: ^5.2.1+1
  http: ^1.0.0

dev_dependencies:
  test: ^1.16.7 # pub run test
  # test_coverage: ^0.4.3             # pub run test_coverage
  # flutter_coverage_badge: ^0.0.1+1  # flutter pub run flutter_coverage_badge
